---
'@backstage/cli-module-maintenance': patch
---

The message printed by `repo fix --check` for packages that are out of sync now suggests the fix command using the detected package manager. No functional change for Yarn projects.
