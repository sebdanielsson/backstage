---
'@backstage/cli-module-migrate': patch
---

Package information for `versions:bump` is now fetched through the package manager API from `@backstage/cli-node`. No functional change for Yarn projects.
