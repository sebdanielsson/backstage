---
'@backstage/create-app': patch
---

Added a `--package-manager` option that creates a pnpm workspace instead of a Yarn one. pnpm is selected automatically when the command runs through pnpm. pnpm 12.4 or later is required.
