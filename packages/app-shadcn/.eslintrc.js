module.exports = require('@backstage/cli/config/eslint-factory')(__dirname, {
  ignorePatterns: ['vite.config.ts'],
  rules: {
    // The shadcn-generated UI components are vendored code without the
    // repository license headers.
    'notice/notice': 'off',
  },
  overrides: [
    {
      // Vendored shadcn/ui components are kept as generated by the shadcn
      // CLI, so repo lint rules that conflict with their code style are
      // disabled for them.
      files: ['src/components/ui/**'],
      rules: {
        '@typescript-eslint/no-shadow': 'off',
        'no-nested-ternary': 'off',
        'no-restricted-syntax': 'off',
        'jsx-a11y/click-events-have-key-events': 'off',
        'jsx-a11y/no-noninteractive-element-interactions': 'off',
        'jsx-a11y/no-autofocus': 'off',
        'react/forbid-elements': 'off',
      },
    },
  ],
});
